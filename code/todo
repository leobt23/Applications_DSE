TODO

- (DONE) Random search invés da gridsearch;
- Guardar imagens, parâmetros e resultados para cada parâmetros em ficheiros separados;
- Models (OneClassSVM, IsolationForest, Autoencoders);
- Preencher hyperparameters;
- Adicionar vários métodos de Undersampling, Oversampling, Over e Under ou nenhum.  
- Approaching for Novel Categories???;
- Testar setup + requirements;
- Mudar validação (15% do 2º dia); 
- Garantir que o modelo é treinado apenas 1x os dois, para validation e teste;
- Separar em ficheiros diferentes;
- Criar versão OOP (com classes);
- Train, validation e teste para os unsupervised;
- Verificar accuracy com e sem preprocessing;
- Implementar cross validation, oversample ou undersample durante cross-validation, not before; 
- Fazer stardardscaler e verificar em que modelos é que funciona melhor e se o PCA o afeta de alguma maneira;
- Advanced approaches (pooling data, ensembling models, etc.)
- Escrever relatório;

your_project/
│
├── data_processing.py
├── model_training.py
├── plotting_saving_utils.py
└── main.py

your_project_name/
│
├── data/
│   ├── raw/                  # Raw data, immutable
│   ├── processed/            # Cleaned and processed data
│   └── external/             # Data from external sources
│
├── notebooks/                # Jupyter notebooks for exploration and presentations
│
├── src/                      # Source code for use in this project
│   ├── __init__.py           # Makes src a Python module
│   ├── data/                 # Scripts to download or generate data
│   │   └── load_data.py
│   ├── features/             # Scripts to turn raw data into features for modeling
│   │   └── build_features.py
│   ├── models/               # Scripts to train models and then use trained models to make predictions
│   │   ├── train_model.py
│   │   └── predict_model.py
│   └── visualization/        # Scripts to create exploratory and results-oriented visualizations
│       └── visualize.py
│
├── tests/                    # Test cases for your project
│   ├── __init__.py
│   └── test_data.py
│
├── latex/                    # Latex cases
│   ├── ...
│   └── ...
│
├── requirements.txt          # The dependencies file for reproducing the analysis environment
├── setup.py                  # Makes project pip installable (pip install -e .) so src can be imported
├── README.md                 # The top-level README for developers using this project
└── .gitignore                # Specifies intentionally untracked files to ignore
